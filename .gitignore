__pycache__/
*.pyc

# 생성물 — Actions 가 빌드해 Pages 아티팩트로 배포한다.
# merged_weekly.csv 도 build_site.py 가 원본 CSV 들로부터 만든다. 커밋해 두면
# 로컬 작업분과 Actions 가 만든 것이 부딪혀 rebase 충돌이 난다(실제로 났다).
index.html
_site/
data/merged_weekly.csv

# PDF 원본 캐시. 20MB 넘고 언제든 다시 받을 수 있다 (CSV 만 커밋한다)
data/newsline_pdf/
